# 🚘 Диаграмма классов

<img src="./Images/Class-Diagram.png" width="680%" />

---

## 📚 Глоссарий

| Понятие                          | Описание                                                                                                                                                                                                                                                                           |
| -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **User**                         | Главный класс приложения. Представляет пользователя системы, который управляет списком автомобилей. Содержит коллекцию `CustomContainer<Car>`, а также методы добавления, редактирования, удаления автомобилей и событий. Отвечает за сохранение и загрузку данных в формате JSON. |
| **Car**                          | Класс, описывающий автомобиль. Имеет поля: `carName`, `carYear`, `carMileage`, `carFuelType`, `carTankVolume`, `carImageFilePath`. Содержит коллекцию событий `CustomContainer<Event>` и объект `Statistics`. Реализует интерфейсы `Presaveable` и `Preloadable` для сериализации. |
| **Event**                        | Абстрактный базовый класс для событий, связанных с автомобилем. Имеет поля: `eventType`, `eventName`, `eventPrice`, `eventDate`, `eventMileage`, `eventComment`. Определяет виртуальные методы для сериализации и получения типа события.                                          |
| **Refueling**                    | Подкласс `Event`, описывающий заправку автомобиля. Добавляет поля `fuelType`, `amountOfLitres`, `pricePerLitre`. Реализует методы `preSaveData()` и `preLoadData()` для сохранения и загрузки данных о заправках.                                                                  |
| **CarService**                   | Подкласс `Event`, описывающий обслуживание автомобиля. Добавляет поле `serviceType` (например, ремонт, диагностика, шиномонтаж). Реализует методы сохранения и загрузки сервисных данных.                                                                                          |
| **Statistics**                   | Класс для расчёта и хранения статистики автомобиля. Содержит данные о суммарных расходах (`carMoneySpent`), а также детализированные показатели по видам сервисов и топливу. Используется в классе `Car`.                                                                          |
| **Validator**                    | Класс для проверки введённых пользователем данных. Использует `CustomException` и `AppDebugLogger` для регистрации ошибок. Проверяет корректность года, пробега, объёма бака, цены, даты и других полей.                                                                           |
| **AppDebugLogger**               | Класс для ведения журнала отладки. Хранит все сообщения об ошибках и событиях. Выводит лог в консоль при завершении работы.                                                                                                                                                        |
| **CustomException**              | Класс пользовательского исключения, используется для обработки ошибок валидации и логики.                                                                                                                                                                                          |
| **CustomContainer<T>**           | Шаблонный контейнер для хранения объектов (аналог вектора с итератором). Реализует собственные методы добавления, удаления и сортировки элементов. Используется для хранения автомобилей и событий.                                                                                |
| **Presaveable**                  | Интерфейс для классов, поддерживающих сериализацию данных перед сохранением. Определяет метод `preSaveData()`.                                                                                                                                                                     |
| **Preloadable**                  | Интерфейс для классов, поддерживающих загрузку данных из сохранённых структур. Определяет метод `preLoadData()`.                                                                                                                                                                   |
| **ChooseCarWindow**              | Класс уровня UI. Отображает список автомобилей (`CarListElementModel`) и позволяет пользователю выбрать или добавить автомобиль.                                                                                                                                                   |
| **EventsWindow**                 | Класс уровня UI. Отображает список событий (`EventListElementModel`) выбранного автомобиля. Позволяет добавлять, редактировать и удалять события.                                                                                                                                  |
| **CarListElementModel**          | Модель представления списка автомобилей для QML-интерфейса. Реализует QAbstractListModel, содержит коллекцию `CarListElement`.                                                                                                                                                     |
| **EventListElementModel**        | Модель представления событий автомобиля для QML-интерфейса. Также основана на QAbstractListModel.                                                                                                                                                                                  |
| **FileDialogHandler**            | Класс-обёртка для вызова стандартных диалогов Qt: открытия и сохранения файлов. Используется при экспорте JSON-отчётов.                                                                                                                                                            |
| **MyApp**                        | Класс, наследующий `QApplication`. Отвечает за жизненный цикл приложения и автоматическое сохранение пользовательских данных при выходе.                                                                                                                                           |
| **EnhancedQmlApplicationEngine** | Расширенная версия `QQmlApplicationEngine` с дополнительными возможностями, например, очисткой кэша QML.                                                                                                                                                                           |

---

